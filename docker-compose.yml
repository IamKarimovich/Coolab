version: '3'
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8080:8080
    depends_on:
      - db
  frontend:
    build:
      context: ./ui
      dockerfile: Dockerfile
    ports:
      - 3000:3000
  db:
    image: 'postgres:13.1-alpine'
    container_name: db
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=krmf0990
      - POSTGRES_DB=coolabdemo